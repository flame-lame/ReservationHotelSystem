public class Bin {
    /*
    System.out.println("---> Guests properties");

    List<Guest> guests = List.of(guest1, guest2, guest3);
    for (int i = 0; i < guests.size(); i++) {
        Guest guest = guests.get(i);
        LocalDate birthDate = guest.getBirthDate();
        DateTimeFormatter formatter = DateTimeFormatter.ofPattern("dd.MM.yyyy");
        String formattedDate = birthDate.format(formatter);
        System.out.println("First name: " + guest.getName() + ", Last name: " + guest.getSurname() + ", Date of birth: " + formattedDate);
    }
    */



}
